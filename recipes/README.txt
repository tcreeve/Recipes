This code assumes that you have
	1. A MySQL server with administrator name=root and password=password.
	2. And have run sampletable.sql in that server
		(Make sure database cooking does not already exist before running).
	3. You can run node.js programs
		a) Go to path/nodejs in Node.js command prompt
		b) Type "node server"
		c) In browser type "http://localhost:8080/view"